% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pcor_test_all.r
\name{pcor_test_all}
\alias{pcor_test_all}
\title{Perform Partial Correlation Tests with Frequentist Methods}
\usage{
pcor_test_all(
  dat,
  control = NULL,
  triangle = c("upper", "lower", "full"),
  alternative = c("two.sided", "less", "greater"),
  method = c("pearson", "kendall", "spearman"),
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{dat}{A data frame or matrix containing numeric variables of interest.}

\item{control}{A data frame, matrix, or numeric vector containing covariates to control for.}

\item{triangle}{Character. Specifies which part of the correlation matrix to return:
\code{"upper"}, \code{"lower"}, or \code{"full"} (default: \code{"upper"}).}

\item{alternative}{Character. Specifies the alternative hypothesis for the frequentist test:
\code{"two.sided"}, \code{"less"}, or \code{"greater"} (default: \code{"two.sided"}).}

\item{method}{Character. Specifies the correlation method for the frequentist test:
\code{"pearson"}, \code{"kendall"}, or \code{"spearman"} (default: \code{"pearson"}).}

\item{conf.level}{Numeric. The confidence level for confidence intervals (default: \code{0.95}).}

\item{cor}{Logical. If \code{TRUE}, computes frequentist correlation coefficients (default: \code{TRUE}).}
}
\value{
A list containing:
\describe{
\item{all}{A data frame with all computed correlation statistics.}
\item{table_XX}{A data frame corresponding to a table named "table_XX",
where "XX" is derived from the output variables (e.g., \code{"table_cor"}, \code{"table_p"}).
The content of the table depends on the provided inputs.}
}
}
\description{
Computes pairwise partial correlations among all variables in a given dataset,
controlling for one or more covariates using frequentist methods.
The function returns correlation estimates along with confidence intervals.
Note: Bayesian methods are not supported in the current version.
}
\examples{
results <- pcor_test_all(mtcars[, 1:3], control = mtcars[, 4:5])
results$all  # View detailed results in a tidy format
results$table_cor  # View partial correlation matrix

}
