% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/t_test_all_tidy.r
\name{t_test_all_tidy}
\alias{t_test_all_tidy}
\title{Perform Frequentist and Bayesian t-tests with Effect Size Estimation from Tidy Data}
\usage{
t_test_all_tidy(
  dataset,
  paired = F,
  var.equal = FALSE,
  mu = 0,
  ci = c("freq", "bayes_central", "bayes_hdi"),
  alternative = c("two.sided", "less", "greater"),
  conf.level = 0.95,
  pd = FALSE,
  bf = FALSE,
  cor = TRUE,
  mean_x_EAP = FALSE,
  mean_x_MAP = FALSE,
  mean_x_MED = FALSE,
  diff_EAP = FALSE,
  diff_MAP = FALSE,
  diff_MED = FALSE,
  cohens_d = NULL,
  cohens_d_EAP = FALSE,
  cohens_d_MAP = FALSE,
  cohens_d_MED = FALSE,
  cohens_dz = TRUE,
  cohens_dz_EAP = FALSE,
  cohens_dz_MAP = FALSE,
  cohens_dz_MED = FALSE,
  rscale_est = Inf,
  rscale_bf = "medium",
  iterations = 10000,
  map_density_n = 512,
  verbose = TRUE
)
}
\arguments{
\item{dataset}{A data frame with exactly three columns, in this order:
\enumerate{
\item Participant ID
\item Independent variable (must have exactly two levels)
\item Dependent variable (numeric)
}}

\item{paired}{Logical. If \code{TRUE}, a paired t-test is performed (default \code{FALSE}).}

\item{var.equal}{Logical. If \code{TRUE}, the two-sample t-test assumes equal variances (default \code{FALSE}).}

\item{mu}{Numeric. Null hypothesis value for the mean difference (default \code{0}).}

\item{ci}{Character vector specifying interval type(s): \code{"freq"}, \code{"bayes_central"},
or \code{"bayes_hdi"}. Default is \code{c("freq","bayes_central","bayes_hdi")}. Passed to \code{t_test_all}.}

\item{alternative}{Character. Alternative hypothesis: \code{"two.sided"}, \code{"less"}, or \code{"greater"}.
Default is \code{"two.sided"}. Passed to \code{t_test_all}.}

\item{conf.level}{Numeric. Confidence/credibility level (default \code{0.95}). Passed to \code{t_test_all}.}

\item{pd, bf, cor}{Logical flags. Whether to compute probability of direction (pd), Bayes factors, or correlation
(for paired samples). Passed to \code{t_test_all}.}

\item{mean_x_EAP, mean_x_MAP, mean_x_MED}{Logical. Report posterior summaries for group means.
Passed to \code{t_test_all}.}

\item{diff_EAP, diff_MAP, diff_MED}{Logical. Report posterior summaries for the mean difference.
Passed to \code{t_test_all}.}

\item{cohens_d}{Logical or character. Type/request for Cohen's d in independent samples. Passed to \code{t_test_all}.}

\item{cohens_d_EAP, cohens_d_MAP, cohens_d_MED}{Logical. Posterior summaries for Cohen's d. Passed to \code{t_test_all}.}

\item{cohens_dz}{Logical. Request Cohen's dz for paired samples. Passed to \code{t_test_all}.}

\item{cohens_dz_EAP, cohens_dz_MAP, cohens_dz_MED}{Logical. Posterior summaries for Cohen's dz. Passed to \code{t_test_all}.}

\item{rscale_est, rscale_bf}{Numeric or character. Cauchy prior scales for estimation and Bayes factors
(e.g., \code{"ultrawide"}, \code{"wide"}, \code{"medium"}, or a positive number).
Defaults are \code{Inf} and \code{"medium"}, respectively. Passed to \code{t_test_all}.}

\item{iterations, map_density_n}{Integer. MCMC iterations and grid size for MAP density. Passed to \code{t_test_all}.}

\item{verbose}{Logical. If \code{TRUE}, print additional messages (default \code{TRUE}).}
}
\value{
The object returned by \code{\link{t_test_all}} (test statistics, effect sizes,
confidence/credible intervals, and Bayesian estimates).
}
\description{
A wrapper around \code{\link{t_test_all}} that accepts tidy-format data frames
with exactly three columns (participant ID, independent variable, dependent variable).
The function automatically reshapes the data (long \eqn{\rightarrow} wide for paired designs),
checks for consistency, and then calls \code{t_test_all()} with the extracted vectors.
}
\details{
This function enforces the following:
\itemize{
\item \code{dataset} must have exactly three columns in the order: ID, independent variable, dependent variable.
\item The independent variable must have exactly two levels.
\item The dependent variable must be numeric.
\item If \code{paired = TRUE}, each participant must contribute exactly one observation in each condition.
}
If any requirement is violated, an informative error is raised.
}
\examples{
set.seed(610)
dat <- data.frame(
  id   = rep(1:10, each = 2),
  cond = rep(c("A","B"), times = 10),
  y    = rnorm(20)
)
# Independent-samples t-test
t_test_all_tidy(dat, paired = FALSE)
# Paired-samples t-test
t_test_all_tidy(dat, paired = TRUE)

}
\seealso{
\code{\link{t_test_all}}
}
